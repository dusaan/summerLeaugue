<h1>Nová správa</h1>

<% form_for(@message) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :Komu %><br />
    <%= f.select :receiver_id, @users.collect { |key, value| [key.name, key.id] }, { :required => true } %>
  </p>
  <p>
    <%= f.label :Predmet %><br />
    <%= f.text_field :subject %>
  </p>
  <p>
    <%= f.label :"Text správy" %><br />
    <%= f.text_area :body %>
  </p>  
  <p>
    <%= f.submit 'Pošli' %>
  </p>
<% end %>

<%= link_to 'Back', messages_path %>
